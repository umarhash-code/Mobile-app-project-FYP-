Data folder

Place dataset files here for model training or evaluation.

Recommended filename: `train_balanced_5moods_extended.csv`.

Note: By default `.gitignore` excludes CSVs in `data/` to avoid committing large raw datasets. If you want to include the CSV in git, remove `data/*.csv` from `.gitignore` before committing.
